({
    buscarERenderizarCadeirasOcupadas: function(component) {

        var classeCSS = 'assentoOcupado';
           
            var cmpTarget = component.find('aoba');
            var auraIdValue = cmpTarget.get("v.aura:id");


    
    }
})
